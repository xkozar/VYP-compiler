MAKEFILE set path to antlr
return in main is just exit
allow empty returns
check void main

NICE TO HAVE:
object type casting (runtime check) and check objects to it's subclasses
variable overlapping(shadowing)
lexical errors
variables cant have same name as functions and classes