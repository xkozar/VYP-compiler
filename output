HELLO
#! /bin/vypint
# VYPcode: 1.0
# Generated by: xkozar02


JUMP main

LABEL main
	#Function start
	SET $SP, $SP + 1
	SET [$SP], $SP
	SET $0, $SP

	#Create variable a
	CREATE $1, 1
	SET $SP, $SP + 1
	SET $SP, $1
	#Create variable res
	CREATE $1, 1
	SET $SP, $SP + 1
	SET $SP, $1
	#Literal expression 5
	SET $SP, $SP + 1
	SET [$SP], 5
	# Variable assignment a
	SET [$SP + 0], [$SP]
	SET $SP, $SP - 1
	#Literal expression 0
	SET $SP, $SP + 1
	SET [$SP], 0
	# Variable assignment a
	SET [$SP + 0], [$SP]
	SET $SP, $SP - 1
	#Literal expression 1
	SET $SP, $SP + 1
	SET [$SP], 1
	#Literal expression 1
	SET $SP, $SP + 1
	SET [$SP], 1

	RETURN [$SP]

LABEL factorial
	#Function start
	SET $SP, $SP + 1
	SET [$SP], $SP
	SET $0, $SP

	#Create variable decremented_n
	CREATE $1, 1
	SET $SP, $SP + 1
	SET $SP, $1
	#Create variable temp_result
	CREATE $1, 1
	SET $SP, $SP + 1
	SET $SP, $1
	# Variable assignment n
	SET [$SP + 0], [$SP]
	SET $SP, $SP - 1
	#Literal expression 2
	SET $SP, $SP + 1
	SET [$SP], 2
	#Literal expression 1
	SET $SP, $SP + 1
	SET [$SP], 1
	# Variable assignment n
	SET [$SP + 0], [$SP]
	SET $SP, $SP - 1
	#Literal expression 1
	SET $SP, $SP + 1
	SET [$SP], 1
	# Variable assignment decremented_n
	SET [$SP + 0], [$SP]
	SET $SP, $SP - 1
	#Literal expression 1
	SET $SP, $SP + 1
	SET [$SP], 1
	#Literal expression 1
	SET $SP, $SP + 1
	SET [$SP], 1
	# Variable assignment n
	SET [$SP + 0], [$SP]
	SET $SP, $SP - 1
	# Variable assignment temp_result
	SET [$SP + 1], [$SP]
	SET $SP, $SP - 1

	RETURN [$SP]

