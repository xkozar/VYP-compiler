#! /bin/vypint
# VYPcode: 1.0
# Generated by: xkozar02

ALIAS FP $0
JUMP main


LABEL printi
	WRITEI [$SP - 2]
    WRITES "\n"
	SUBI $SP, $SP, 2
	RETURN [$SP + 2]



LABEL prints
	WRITES [$SP - 2]
    WRITES "\n"
	SUBI $SP, $SP, 2
	RETURN [$SP + 2]


LABEL main
	#Function start
	ADDI $SP, $SP, 1
	SET $FP, $SP

	#Create variable s
	CREATE $1, 1
	SET [$FP+ 0], $1

	# Restore stack pointer
	ADDI $SP, $FP, 1

	#String literal expression "ahoj"
	CREATE $6, 1
	SETWORD $6, 0, "ahoj"
	GETWORD $6, $6, 0
	SET [$SP], $6
	ADDI $SP, $SP, 1

	# Variable assignment s
	SUBI $SP, $SP, 1
	SET [$FP+ 0], [$SP]

	# Restore stack pointer
	ADDI $SP, $FP, 1

	# Variable expression
	SET [$SP], [$FP+ 0]
	ADDI $SP, $SP, 1

	# Variable expression
	SET [$SP], [$FP+ 0]
	ADDI $SP, $SP, 1

	# Variable expression
	SET [$SP], [$FP+ 0]
	ADDI $SP, $SP, 1

	# Calling function prints
	SET [$SP], $FP
	ADDI $SP, $SP, 1
	CALL [$SP], prints

	# Calling function prints
	SET [$SP], $FP
	ADDI $SP, $SP, 1
	CALL [$SP], prints

	# Calling function prints
	SET [$SP], $FP
	ADDI $SP, $SP, 1
	CALL [$SP], prints

	# Restore stack pointer
	ADDI $SP, $FP, 1


	JUMP __END


LABEL __END
