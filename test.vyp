class Hello: Object {
    void test(void){
        super.test().me.please()
    }
}

/*
class Shape : Object {
    int id;
    void Shape(void) {
        id = 5;
        print("constructor of Shape");
    }
    string toString(void) {
        //return "instance of Shape " + (string)(this.id);
        return "instance of Shape " + (string)(5);
    }
}
class Rectangle : Shape {
    Shape s;
    int height, width;
    string toString(void) { return super.toString()
    + " - rectangle " + (string)(this.area()); }
    int area(int x, int y) { return this.s.id * this.width; }
}

string testicek(int x, int y, string s) {
    testicek(123,456, "2");
    int test, x;
    test = 5 + 1 * 5;
    x = test + 8;
    x = !-5;
    x = test(1, 1);
    //int test;
}

int test(int a, int b) {
    int x;
}


void main(void) { // Program Main function
    int a, res;
    print("Enter an integer to compute its factorial:");
    a = readInt();
    if (a < 0) {
        int xxx;
        print("\nFactorial of a negative integer is undefined!\n"); }
    else {
        res = 1;
        while (a > 0) {
            res = res * a; a = a - 1;
        } // endwhile
        print("\nThe result is: ", res, "\n");
    } // endif
} // main
*/